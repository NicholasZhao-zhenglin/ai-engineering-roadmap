1. 层序遍历(102)
   思路：使用队列（先进先出）。标准的BFS（广度优先搜索）题目，特征是使用队列作为辅助数据结构
   （1） 将起点加入队列，标记为已访问
   （2）当队列不为空时：
     a. 取出队首节点 u
     b. 访问/处理 u
     c. 将 u 的所有未被访问的邻居加入队列，标记已访问
   （3） 重复步骤 2，直到队列为空
   要区分值（存在列表）里和节点（存在队列里）
